function selectionSort(arr) {
    let n = arr.length;

    for (let i = 0; i < n - 1; i++) {
        let minIndex = i;
    }
  
}

// Example Usage:
let arr = [9, 5, 1, 4, 3];
console.log(selectionSort(arr)); 
// Output: [1, 3, 4, 5, 9]
